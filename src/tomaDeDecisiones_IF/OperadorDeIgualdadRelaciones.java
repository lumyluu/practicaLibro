package tomaDeDecisiones_IF;
// Si la condición en una instrucción if es verdadera, se ejecuta el cuerpo de la instrucción if.
// Si la condición es falsa, no se ejecuta el cuerpo

import java.util.Scanner;
public class OperadorDeIgualdadRelaciones {
    public static void main (String [] args ){
    Scanner scanner = new Scanner (System.in);

         // operadores de igualdad (== y !=)
             // Ejemplo: x == y ( X es igual a Y)
            // x != y ( X no es igual a Y)

        // operadores relacionales (>, <, >= y <=)
            // Ejemplo: X > Y ( X es mayor que Y )
            // X < Y ( X es menor que Y)
            // X >= Y ( X es mayor o igual que Y)
            // X <= Y ( X es menor o igual que Y)



    }

}
